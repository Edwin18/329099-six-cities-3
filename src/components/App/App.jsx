import React from 'react';
import Main from '../Main/Main.jsx';

// eslint-disable-next-line react/prop-types
const App = ({available}) => {
  return <Main
  // eslint-disable-next-line react/prop-types
    available={available}
  />;
};

export default App;
